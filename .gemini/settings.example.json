{
  "mcpServers": {
    "liaison-supabase-db": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://postgres:<LOCAL_DB_PASSWORD>@db.<project-ref>.supabase.co:5432/postgres"
      ],
      "description": "Local-only template. Copy to settings.json outside source control and replace placeholders with your own local credentials. Never commit the populated file."
    },
    "liaison-visual-auditor": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-puppeteer"
      ],
      "description": "PPEE Vision Engine template entry. Safe to keep in source control."
    }
  }
}